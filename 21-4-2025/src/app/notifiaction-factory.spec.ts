import { NotifiactionFactory } from './notifiaction-factory';

describe('NotifiactionFactory', () => {
  it('should create an instance', () => {
    expect(new NotifiactionFactory()).toBeTruthy();
  });
});
